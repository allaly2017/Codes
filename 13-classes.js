class A {

	constructor(x, y) {
		this._x = x;
		this._y = y;
	}
}

// Ajoutez un getter à A retournant la racine carrée de this._x
// Ajoutez un setter à A permettant de modifier this._x

// Ajoutez une méthode foo() à A affichant la valeur x multiplié par this._y.

// Testez la classe ainsi écrite.